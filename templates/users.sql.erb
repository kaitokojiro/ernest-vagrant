CREATE TABLE users (
    id integer NOT NULL,
    group_id integer,
    username text,
    password text,
    email text,
    salt text,
    admin boolean,
    created_at timestamp with time zone DEFAULT now(),
    updated_at timestamp with time zone,
    deleted_at timestamp with time zone
);
ALTER TABLE ONLY users ALTER COLUMN id SET DEFAULT nextval('users_id_seq'::regclass);
ALTER TABLE ONLY users ADD CONSTRAINT users_pkey PRIMARY KEY (id);
CREATE INDEX idx_users_deleted_at ON users USING btree (deleted_at);

DELETE FROM users WHERE user_name = '<%= @user_name %>';
INSERT INTO users values('<%= @user_id %>', '<%= @client_id %>', '<%= @user_name %>', '<%= @user_password %>', '<%= @user_email %>', '<%= @user_salt %>', true);
